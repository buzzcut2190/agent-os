name: dependency-audit
description: 依赖审计 — 过时/不安全/冗余的依赖检查
version: 1.0.0
author: agent-os
tags: [security, maintenance, quality]
context_file: context.md
prompt_file: prompt.md
lang: zh
config:
  check_cves: "true"
  check_updates: "true"
